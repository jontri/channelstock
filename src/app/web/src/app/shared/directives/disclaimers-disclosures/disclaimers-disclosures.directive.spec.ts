import { DisclaimersDisclosuresDirective } from './disclaimers-disclosures.directive';

describe('DisclaimersDisclosuresDirective', () => {
  it('should create an instance', () => {
    const directive = new DisclaimersDisclosuresDirective();
    expect(directive).toBeTruthy();
  });
});
