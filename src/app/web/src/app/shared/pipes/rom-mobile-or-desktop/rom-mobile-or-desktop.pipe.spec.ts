import { RomMobileOrDesktopPipe } from './rom-mobile-or-desktop.pipe';

describe('RomMobileOrDesktopPipe', () => {
  it('create an instance', () => {
    const pipe = new RomMobileOrDesktopPipe();
    expect(pipe).toBeTruthy();
  });
});
